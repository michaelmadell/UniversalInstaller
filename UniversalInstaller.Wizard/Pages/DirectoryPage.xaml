<UserControl x:Class="UniversalInstaller.Wizard.Pages.DirectoryPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="700">
    <Grid Background="White" Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Select Installation Location"
                   FontSize="18"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1"
                   x:Name="DirectoryMessage"
                   TextWrapping="Wrap"
                   Margin="0,0,0,20"/>

        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="DirectoryTextBox"
                     Grid.Column="0"
                     Height="25"
                     VerticalContentAlignment="Center"
                     Margin="0,0,10,0"/>

            <Button Grid.Column="1"
                    Content="Browse..."
                    Width="80"
                    Height="25"
                    Click="BrowseButton_Click"/>
        </Grid>

        <StackPanel Grid.Row="3" VerticalAlignment="Bottom">
            <TextBlock x:Name="SpaceRequiredText"
                       FontSize="11"
                       Foreground="Gray"
                       Margin="0,0,0,5"/>
            <TextBlock x:Name="SpaceAvailableText"
                       FontSize="11"
                       Foreground="Gray"/>
        </StackPanel>
    </Grid>
</UserControl>
